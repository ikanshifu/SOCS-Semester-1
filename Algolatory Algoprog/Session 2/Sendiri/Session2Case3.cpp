#include <stdio.h>
#include <string.h>

int main(){
	int T;
	scanf("%d", &T);
	for(int tc = 1;tc<=T;tc++){
		char x[1005];
		scanf("%s",x);
		int arr[strlen(x)];
	
		for(int i=0;i<strlen(x);i++){
			if(x[i]=='B'|| x[i]=='C')
		
	
	}
	return 0;
}
